const heading = React.createElement('customTag', {'id': 'heading', 'name': 'Ashish'}, 'Hello World From React!');

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(heading);
